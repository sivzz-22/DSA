class Solution {
    public int removeDuplicates(int[] nums) {
        int j=0;
        for(int i=1;i<nums.length;i++){
            if(nums[i]!=nums[j]){
                j++;
                nums[j]=nums[i];
            }
        }
        return j+1;
    }
}



/*

Two pointer apporach

1 st we need to set 'j' as the unique value 
Then we need to compare the j with the next element i ...If both are same we increment j....
If i anf j are not same...They are unique..we need to change the unique value i to the next i++
then the value of j is assigned to i ....then j incremented

-------------------------------------------------------------------------------------------------------

Time complexity - O(n)
Space complexity - O(1)


*/
