class Solution {
    public void reverseString(char[] s) {
        int i = 0;
        int j = s.length-1;
        while(i<j){
            char temp = s[i];
            s[i]=s[j];
            s[j]=temp;
            i++;
            j--;
        } 
    }
}

/*

Two pointer apporach :

Creating two variables i and j
swaping 
i<j condition in while loop

Time Complexity = 0(n)
Space Complexity = 0(1)
-------------------------------------------------------------


arr.length -> used to calculate the length..
arr.length() -> used to calculate length for a string 
arr.size() -> used to calculate the size of the collections 

*/